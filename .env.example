# Used by docker-compose.yml for the local PostgreSQL container.
# Copy to .env (repo root) and choose your own local password.
POSTGRES_USER=commerceops
POSTGRES_PASSWORD=change-me
POSTGRES_DB=commerceops
POSTGRES_PORT=5432
